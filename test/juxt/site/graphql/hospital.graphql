schema {
  query: Query
}

type Query {
  patients: [Patient] @site(action: "https://example.org/actions/get-patients")
}

type Patient {
  name: String
  heartRate: Int @site(action: "https://example.org/actions/read-vitals")
}
