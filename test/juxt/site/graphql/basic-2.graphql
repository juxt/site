directive @site(
  action: [String!]
) on FIELD_DEFINITION

schema {
  query: Query
}

type Query {
  patients: [Patient] @site(action: ["https://site.test/actions/get-patients"])
}

type Patient {
  name: String
}
