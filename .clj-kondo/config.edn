{:linters
 {:unused-namespace {:exclude [clojure.tools.logging
                               clojure.pprint
                               juxt.site.logging
                               juxt.site.repl]}
  :unused-referred-var {:exclude {juxt.test.util [with-fixtures]
                                  clojure.pprint [pprint]
                                  juxt.site.logging [with-logging]
                                  clojure.test [deftest is are]}}}}
