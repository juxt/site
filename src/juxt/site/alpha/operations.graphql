query GetRequest($siteUri: ID) {
  request(id: $siteUri) {
    id
    status
    date
    requestUri
    method
    operationName
    errors {
      __typename
      message
      stackTrace {
        methodName
        fileName
        lineNumber
        className
      }
      exData
      ... on SiteGraphqlExecutionError {
        storedQueryResourcePath
        operationName
        variables
        fieldErrors {
          message
          locations { line column }
          path
          stackTrace
          extensions
        }
      }
    }
    _detail
  }
}
